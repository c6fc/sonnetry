{
	local sonnetry = self,

	bootstrap(project):: std.native("bootstrap")(project),
	envvar(name):: std.native("envvar")(name),
	remote(project, resource):: std.native("remote")(project, resource),
	path():: std.native("path")()
}